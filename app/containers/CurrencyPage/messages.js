/*
 * CurrencyPage Messages
 *
 * This contains all the text for the CurrencyPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CurrencyPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CurrencyPage container!',
  },
});
