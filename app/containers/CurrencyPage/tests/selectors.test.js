// import { selectCurrencyPageDomain } from '../selectors';

describe('selectCurrencyPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
