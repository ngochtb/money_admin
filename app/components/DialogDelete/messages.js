/*
 * DialogDelete Messages
 *
 * This contains all the text for the DialogDelete component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DialogDelete';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DialogDelete component!',
  },
});
